# --- LLM API Keys (Optional) ---
# OPENAI_API_KEY=your_openai_api_key_here
# GOOGLE_API_KEY=your_google_api_key_here

# --- LangSmith Tracing (Optional) ---
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_ENDPOINT=https://api.smith.langchain.com
# LANGSMITH_API_KEY=your_langsmith_api_key_here
# LANGCHAIN_PROJECT=your_langsmith_project_name_here # Optional: Defaults to 'default'

# --- LLM Model Names (Optional - Defaults shown) ---
# OLLAMA_MODEL=llama3.2:latest
# OPENAI_MODEL=gpt-4o
# GEMINI_MODEL=gemini-1.5-flash
# EMBEDDING_MODEL=text-embedding-ada-002
# AVAILABLE_LLMS=Ollama (Local),OpenAI (API),Gemini (API) # Comma-separated list for UI

# --- ChromaDB Path (Optional - Default shown) ---
# CHROMA_DB_PATH=/app/chroma_db

# --- MCP Server Details (Optional - Defaults shown) ---
# Postgres
# MCP_POSTGRES_COMMAND=node
# MCP_POSTGRES_SCRIPT_PATH=c:/___WORK/ModelContextProtocolPostgree/mcp_servers/modelcontextprotocol-servers/src/postgres/dist/index.js
# MCP_POSTGRES_CONN_STRING=postgresql://postgres:mysecretpassword@localhost:5432/postgres

# Filesystem (Docker based)
# MCP_FILESYSTEM_COMMAND=docker
# MCP_FILESYSTEM_IMAGE=mcp/filesystem
# MCP_FILESYSTEM_MOUNT_SOURCE_PREFIX=mcp_logs # Host directory prefix (date added automatically)
# MCP_FILESYSTEM_MOUNT_TARGET=/data # Target inside container
# MCP_FILESYSTEM_SERVER_ROOT=/data # Server root inside container

# --- MCP Filesystem Paths (Optional - Defaults shown, relative to MOUNT_TARGET) ---
# MCP_LOG_FILE_PATH=output.txt
# MCP_SAVED_SQL_PATH=successful_queries.sql

# --- Gmail (Optional - Not currently used by agent logic) ---
# GMAIL_CREDENTIALS_FILE=credentials.json
# GMAIL_TOKEN_FILE=token.json
